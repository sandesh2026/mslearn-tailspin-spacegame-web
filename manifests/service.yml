apiVersion: v1
kind: Service
metadata:
    name: "myaks-df30"
    labels:
        app: "myaks-df30"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myaks-df30"